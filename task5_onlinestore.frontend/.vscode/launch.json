{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "firefox",
      "request": "launch",
      "name": "localhost (Firefox)",
      "url": "https://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
